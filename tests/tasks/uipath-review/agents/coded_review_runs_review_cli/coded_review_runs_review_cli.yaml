task_id: skill-review-agents-coded-runs-review-cli
description: >
  Delegation smoke for the CLI-first review contract on a coded agent.
  pre_run scaffolds a clean coded FUNCTION agent (main.py + pyproject.toml
  + uipath.json + entry-points.json). The reviewer must classify the
  project as a coded agent, run `uip codedagent review --output json`
  (Step 2.5a) to delegate deterministic detection to the CLI, read the
  judgment catalog (agents-coded-rules.md / agents-common-rules.md), stay
  read-only, and produce a report. Deterministic findings are the CLI's
  responsibility (covered in the CLI repo), so no specific rule is asserted.
tags: [uipath-review, smoke, mode:diagnose, lifecycle:discover]

run_limits:
  turn_timeout: 1800
  task_timeout: 2400
  max_turns: 40

sandbox:
  driver: tempdir
  python: {}

pre_run:
  - command: "python3 $SKILLS_REPO_PATH/tests/tasks/uipath-review/agents/coded_review_runs_review_cli/inject_clean_function_agent.py"
    timeout: 30

initial_prompt: |
  A UiPath coded agent project exists at `CodedAgent` in the current
  directory. Review the agent project and produce a structured review
  report with Critical / Warning / Info findings.

  This is a read-only review. Do NOT modify, create, or delete any
  files inside `CodedAgent/`. Do NOT scaffold a new project. Do NOT
  ask for approval, confirmation, or feedback.

  Important — produce the review in TWO places, identical content:

  1. As the chat output (the normal review surface), AND
  2. Save the SAME report to a file at `./_review_report.md` at the
     sandbox root (NOT inside `CodedAgent/`).

  Writing the file copy first, before producing the chat report, is
  encouraged — that way the file persists even if the chat response is
  cut off. The file is the source of truth the test harness will check.

success_criteria:
  - type: skill_triggered
    description: "Agent invoked the uipath-review skill"
    skill_name: "uipath-review"
    expected_skill: "uipath-review"
    weight: 3.0

  - type: command_not_executed
    description: "Read-only contract: no Write inside CodedAgent/ (Critical Rule 1)"
    tool_name: "Write"
    command_pattern: '"file_path":\s*"[^"]*CodedAgent'
    weight: 3.0

  - type: command_not_executed
    description: "Read-only contract: no Edit inside CodedAgent/ (Critical Rule 1)"
    tool_name: "Edit"
    command_pattern: '"file_path":\s*"[^"]*CodedAgent'
    weight: 3.0

  - type: command_executed
    description: "Ran the review CLI first (Step 2.5a): uip codedagent review --output json"
    tool_name: "Bash"
    command_pattern: 'uip\s+codedagent\s+review'
    min_count: 1
    weight: 3.0
    pass_threshold: 1.0

  - type: run_command
    description: "A non-trivial review report was produced"
    command: "python3 $TASK_DIR/check_coded_review_runs_review_cli.py"
    timeout: 30
    expected_exit_code: 0
    weight: 2.0
    pass_threshold: 1.0
